{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["VERCEL_ENV"],
      "inputs": ["package.json", "src/**"],
      "outputs": ["dist"],
      "cache": true
    },
    "version:bump": {
      "inputs": ["src"],
      "outputs": ["docs"]
    }
  }
}
