{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["VERCEL_ENV", "VC_TUNNEL_SERVER_INSTALLER"],
      "inputs": ["package.json", "src/**", "../pty-tunnel-server/public"],
      "outputs": ["dist"],
      "cache": true
    },
    "version:bump": {
      "inputs": ["src"],
      "outputs": ["docs"]
    }
  }
}
