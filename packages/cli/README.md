# @vercel/sandbox-cli (Deprecated)

**This package is deprecated.** Please use [`sandbox`](https://www.npmjs.com/package/sandbox) instead.

## Migration

Replace `@vercel/sandbox-cli` with `sandbox`:

```bash
pnpm remove -g @vercel/sandbox-cli
pnpm add -g sandbox
```
