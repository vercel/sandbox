---
"@vercel/sandbox": minor
---

Add `@vercel/sandbox/mock` subpath export — a drop-in mock backed by `just-bash` for realistic command execution out-of-the-box, with MSW-style `command()` handler overrides via `setupSandbox()`
