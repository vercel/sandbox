---
"sandbox-image-node": patch
---

Pin exact Node.js versions during builds, publish exact-version image tags alongside the major-version tags, and check nightly for updates to each configured major release line.
