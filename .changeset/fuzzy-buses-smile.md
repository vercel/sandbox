---
"@vercel/sandbox": minor
---

Add an MSW-style command handler API to the mock sandbox with `setupSandbox()` defaults, per-instance handler overrides, and a `command()` matcher factory while preserving `commands` map fallback behavior.
