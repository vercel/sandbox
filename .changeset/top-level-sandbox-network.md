---
"sandbox": minor
---

Add a top-level network command to attach or detach a sandbox from Secure Compute. It appears in sandbox --help and reuses the existing config network-id behavior. Explain the Enterprise plan requirement in root help, network configuration help, and network examples.
